// Copy this file to secrets.h and fill in your values.
// secrets.h is in .gitignore and will NOT be committed.
//
// If you use WiFi provisioning (captive portal), you can leave
// these as empty strings — the device will start an AP for setup.

#pragma once

// Default admin credentials for HTTP Basic Auth and OTA.
// Change these after first login via the web panel.
#define DEFAULT_ADMIN_USER  "admin"
#define DEFAULT_ADMIN_PASS  "admin"
